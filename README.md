# ToDo-App-Frontend
A cross-platform Todo app built with React Native (Expo) and TypeScript, featuring JWT-free session handling via AsyncStorage, dark/light theming, animated task interactions, and custom toast/confirm UI  connected to a Java backend REST API.
